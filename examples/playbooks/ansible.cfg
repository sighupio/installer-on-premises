[defaults]
inventory = hosts.yaml
host_key_checking = False
roles_path = ../../roles
timeout = 90
# Format the default callback output as YAML.
callback_result_format = yaml
# Use the stdout_callback when running ad-hoc commands.
bin_ansible_callbacks = True